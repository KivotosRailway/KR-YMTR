# 自定义轨道数据

YMTR 新增的轨道数据编辑器（Rail Data Editor）允许给每条轨道附加任意键值对数据。
数据随 MTR 存档保存、随网络同步；ANTE 加载时自动镜像到其 customConfigs，ANTE JS 同样可读。

## 玩家操作：给轨道写入数据

1. 手持「轨道数据编辑器」右键轨道节点
2. 点击「+ 添加数据」，填写：
   - **名称**：数据键名（如 `catenaryheight`）
   - **类型**：`string` / `int` / `float` / `bool`
   - **值**：数据值（按字符串存储）
3. 点击「保存」

同一节点连接多条轨道时，用 ◀ ▶ 按钮切换要编辑的轨道。

## JS 读取（列车脚本）

`render(ctx, state, train)` 中读取当前列车所在轨道的数据：

示例（接触网高度）：

```js
let idx = train.getRailIndex(train.railProgress(), true); // 当前路径段索引
let seg = train.path().get(idx);                          // 当前段 PathData
let value = seg.rail.getRailData().get("catenaryheight"); // 读取值（String，未设置时为 null）
if (value !== null) {
    let h = parseInt(value);                              // 按 int 解析
    // ... 使用 h
}
```

## JS 读取（轨道物件脚本）

`render(ctx, state, rail)` 中 `rail` 是 RailWrapper：

```js
let value = rail.mtrRail().getRailData().get("键名");
```

## 存储格式

每条数据在轨道上存两个键：

| 键 | 值 |
|---|---|
| `键名` | 值字符串（JS 直接读） |
| `__type__键名` | 类型标记（`string`/`int`/`float`/`bool`） |

类型信息仅供编辑器与脚本参考，JS 读取后自行按需解析即可（`parseInt` / `parseFloat` / 布尔比较）。


ANTE 加载时，同一份数据自动镜像到 ANTE 的 `customConfigs`，因此以下写法同样有效：

```js
let value = seg.rail.getCustomConfigs().get("catenaryheight");
```

但仍推荐统一使用 `getRailData()`，MTR 原生方法。

## 注意事项

- 键名不含空格；空键名的条目不会保存
- 同名键重复添加时，后者覆盖前者
- 修改后必须点「保存」才生效并写入存档
- 读取时务必判空（`null`），轨道未设置数据是正常情况
